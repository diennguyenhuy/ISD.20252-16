spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/aims
    username: postgres
    password: ${DB_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 587
    username: aims.ict16@gmail.com
    password: ${APP_EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    user:
      name: aims_admin
      password: ${SECURITY_PASSWORD}

logging:
  level:
    com.hust.soict.ict.aims: debug

vietqr:
  username: customer-maybeez321-user26591
  password: ${VIETQR_PASSWORD}
  apiBaseUrl: https://dev.vietqr.org/vqr/api
  accountNo: 03349966868
  accountName: NGUYEN TIEN DAT
  bankCode: MB
  expectedpassword: 12345qwertW
  expectedusername: maybeez

paypal:
  base-url: https://api-m.sandbox.paypal.com      # live: https://api-m.paypal.com
  client-id: AdMTfa7iXh-wBCivX8N05MMJFfxkk3FRML_-g7dRW4QB6PGH_Rgoipj-BRktMyuxiZPGiCEkVdpZJ0tw
  client-secret: EIyRBDJ0E3BqqfPumeC1uokmAotH9HUyNMJd3UpLxCYpXVh5lM1mqhGc-99KJ3mo_1uAZlkgwseCS4DZ

  # PayPal sandbox does not settle VND, so AIMS VND prices are converted for the
  # gateway only. Adjust rate as needed; the converted value never leaves the subsystem.
  currency: USD
  vnd-to-currency-rate: 25000

  brand-name: AIMS

  # Frontend routes PayPal redirects the customer back to. ${APP_FRONTEND_URL}
  # should be your SPA origin (e.g. http://localhost:5173 in dev).
  return-url: ${APP_FRONTEND_URL:http://localhost:5173}/checkout/payment/paypal/callback
  cancel-url: ${APP_FRONTEND_URL:http://localhost:5173}/checkout/payment/paypal/cancel

app:
  frontend:
    url: ${APP_FRONTEND_URL:http://localhost:5173}

aims:
  app:
    jwtSecret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    jwtExpirationMs: 86400000