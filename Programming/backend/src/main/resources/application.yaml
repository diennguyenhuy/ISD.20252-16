spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/aims
    username: postgres
    password: ${DB_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 587
    username: test@aims.com
    password: test

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true