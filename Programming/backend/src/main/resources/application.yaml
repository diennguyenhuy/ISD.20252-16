spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/aims
    username: postgres
    password: ${DB_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 587
    username: aims.ict16@gmail.com
    password: ${APP_EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    user:
      name: aims_admin
      password: ${SECURITY_PASSWORD}

logging:
  level:
    com.hust.soict.ict.aims: debug

vietqr:
  username: customer-maybeez321-user26591
  password: ${VIETQR_PASSWORD}
  apiBaseUrl: https://dev.vietqr.org/vqr/api
  accountNo: 03349966868
  accountName: NGUYEN TIEN DAT
  bankCode: MB
